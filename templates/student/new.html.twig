{% extends 'base.html.twig' %}

{% block title %}Nouvel étudiant{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h3 class="card-title">Créer un nouvel étudiant</h3>
                </div>
                <div class="card-body">
                    {{ include('student/_form.html.twig') }}
                </div>
                <div class="card-footer text-end">
                    <a href="{{ path('app_student_index') }}" class="btn btn-secondary">Retour à la liste des étudiants</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}